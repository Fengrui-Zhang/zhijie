# === 数据库 (必填) ===
DATABASE_URL="postgresql://user:password@localhost:5432/zhijie?schema=public"

# === NextAuth 认证 (必填) ===
# 用于 JWT 签名，可通过 openssl rand -base64 32 生成
NEXTAUTH_SECRET="your-secret-key-here"
# 应用 URL（生产环境改为实际域名）
NEXTAUTH_URL="http://localhost:3000"

# === DeepSeek API (必填) ===
DEEPSEEK_API_KEY="sk-xxx"

# === 元奋居 API (必填) ===
YUANFENJU_API_KEY="your-yuanfenju-key"

# === 知识库 Embedding (可选，不填则使用 DeepSeek) ===
# EMBEDDING_PROVIDER="openai"
# EMBEDDING_API_KEY=""
# EMBEDDING_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
# EMBEDDING_MODEL="text-embedding-v3"

# === 知识库检索 (可选) ===
# USE_HIER_KNOWLEDGE="true"
